/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  swcMinify: false,
  // images: {
  //   loader: 'imgix',
  //   path: ['https://gateway.ipfscdn.io', 'localhost:3000'],
  // },
}

module.exports = nextConfig